# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

ユーザーは日本人なので日本語で対話してください。

## プロジェクト概要

**TRPGシナリオ執筆エディタ** — エモクロアTRPG向けに、シナリオ本文・技能判定・共鳴判定・NPCデータをひとつの画面で書けるエディタ。書いたそばからプレビューに反映され、そのままPDF出力できる。

技術スタック: 素のHTML + CSS + JavaScript（フレームワーク・ビルドツール・パッケージマネージャを一切使わない単一ファイル構成）。外部依存は Google Fonts のCSS1本のみ。

## コマンド

```bash
# ビルド工程・依存パッケージ・テストランナーは無い。index.html を開けば動く。
start index.html                # 既定のブラウザで直接開く（Windows）

# file:// と http:// では挙動が変わる箇所があるため、確認はHTTP経由で行う
python -m http.server 8000      # http://localhost:8000/ で配信

# 設定の確認
perl scripts/devenv/check-doc-sync.pl            # コミットゲートを手動実行
perl scripts/devenv/check-doc-sync.pl --summary  # ルール一覧をMarkdown表で出力
perl scripts/devenv/setup.pl --version           # devenv の版
```

`git commit` 時には pre-commit フックが走る。内容と追加方法は「コミット時のチェック」を参照。

## アーキテクチャ

このアプリは **`index.html` 1枚**で完結する。HTML・CSS・JavaScript が全て同居しており、ビルド工程も依存パッケージも無い。ブラウザで開けばそのまま動く。

**なぜ1枚構成なのか。** セッション中のGM（ゲームマスター）が使う道具であり、「渡せば即動く」ことを最優先しているため。ビルドを挟むと配布と改造の敷居が上がる。この制約があるので、npmパッケージの導入やファイル分割を提案する前に、単一ファイルで済まないかを先に検討すること。

### 永続化層

`index.html` 内の `API` オブジェクトが**データ永続化の唯一の窓口**である。UI側のコードは保存先を知らず、ここを差し替えるだけで保存先を変えられる。保存に関わる変更は必ずこの層で行い、UI側から直接 localStorage を触らないこと。

`localStorage` には**役割の違う3系統**のキーが入る。混同すると「保存したのに一覧に出ない」類の不具合を作り込む。

| キー | 役割 |
|---|---|
| `trpg_mock_db_scenarios` | シナリオの**保存庫**。一覧・読込・削除の対象 |
| `trpg_current_id` / `trpg_scenario_content` / `trpg_scenario_title` / `trpg_npcs_cache` | **編集中の作業状態**。ブラウザを閉じても続きから再開するためのもの |
| `trpg_shortcuts` | ショートカットキーの割当 |

### 保存の2系統

- **自動保存** — 入力のたびに `handleInput()` が走り、500msのデバウンス後に `saveToLocal()` と `updatePreview()` を呼ぶ。これは「編集中の作業状態」を書くだけで、シナリオ保存庫には触れない。
- **明示保存** — 保存ボタンまたはショートカットからのみ `API.saveScenario()` が呼ばれ、シナリオ保存庫へ書き込まれる。

**この2つは別物である。** 自動保存されていてもシナリオ一覧には現れない。

### 独自記法のプレビュー

`parseMarkdown()` から `parseElement()` が、Markdown風の独自記法をプレビュー用HTMLへ変換する。エモクロアTRPG固有の記法（技能判定・共鳴判定・NPCカード・情景描写・DL情報）を持ち、**標準Markdownとは互換ではない**。

記法を追加するときは、対応する箇所が3つある。1つでも漏れると「ボタンからは入るがプレビューに出ない」状態になる。

1. `parseMarkdown()` / `parseElement()` — 記法をHTMLに変換する
2. `insertTemplate()` — 下部パネルのクイック挿入ボタンから記法を挿入する
3. `DEFAULT_SHORTCUTS` — ショートカットキーの割当と、設定画面に出るラベル

### Code.gs（旧バックエンド）

`Code.gs` は Google Apps Script 版のバックエンドで、Googleスプレッドシートをデータベースとして使う。**現在は使っていない**（「注意事項」の脱却方針を参照）。


## 実装前の参照ルール

実装に着手する前に、必ず以下を確認すること。

1. **`docs/` 配下** — このプロジェクト自体の仕様書・設計書・運用方針。実装対象の機能に関連するものを読んでから着手する。
2. **`knowledge/` 配下** — 外部ツールの仕様・TRPGルールの調査結果などのリファレンス資料。実装方針が不明な場合や外部仕様を確認する場合に参照する。目次は `knowledge/INDEX.html`。
   **このフォルダは `.gitignore` で除外されており、リポジトリには含まれない**（TRPGルールブック由来の資料を含むため）。ローカルの作業ディレクトリにのみ存在する。clone した環境には無いので、`knowledge/` の内容を前提にした手順を書かないこと。

### `docs/` と `knowledge/` の役割分担

| | 置くもの | 形式 |
|---|---|---|
| `docs/` | このプロジェクト自体の仕様・設計・運用方針 | 番号付きHTML。共通CSS `docs/assets/style.css` を参照 |
| `knowledge/` | 外部ツール仕様・調査結果・ドメイン知識 | HTML（生データサンプルはネイティブ形式のまま）。**gitignore済みでリポジトリ外** |

HTMLで持つのは、構造化により情報密度が高く、人間・エージェントの双方が内容を把握しやすいため。
新規ドキュメントの作り方は `.claude/skills/doc-html-writer/SKILL.md` を参照。

## ドキュメントの整合性維持

このプロジェクトではAIエージェントが実装作業を行うため、`docs/` が実装から乖離しないことが重要である。

- **同一のルールは1箇所にだけ書く。** 新しいルールを追加する際は、既存の `CLAUDE.md` / `docs/` に類似のものが無いか確認し、追加する場合は1箇所のみに記載して、他からはリンクを置く。
  同じ説明を2箇所以上に書こうとしているなら、それは「集約先のドキュメントが不足している」サインである。
- **新規ドメインに注意する。** コミット時のチェックは「既にあるドキュメントの更新漏れ」しか検知できない。既存ドキュメントに該当箇所が1つも無い領域（＝完全な新規ドメイン）は、全チェックを満たしたままドキュメント未整備で通過してしまう。
  実装前に対象機能の主要な名詞で `docs/` を検索し、**ヒットが0〜1ファイルなら新規ドメインとして扱う**こと。その場合は設計書を新規作成すべきかを必ず検討する。新規設計書が担うのは主に「**なぜその設計にしたか**」（検討した代替案と却下理由、想定した攻撃と防御、将来拡張の入り口）である。
- **設計判断の根拠がリポジトリ内に残るか確認する。** 会話や一時的な計画ファイルにしか無い状態は、実質的に何も残っていないのと同じである。

## コミット時のチェック

pre-commit フック（`.githooks/pre-commit`）から `scripts/devenv/check-doc-sync.pl` が走り、`devenv.config.json` の `syncRules` を評価する。実行に必要なのは git と Perl だけで、Node・npm は要らない。

現在有効なルール:

<!-- この表は `perl scripts/devenv/check-doc-sync.pl --summary` で再生成できる。手で書き足さないこと -->

| ID | 種別 | 変更を検知する対象 | 同じコミットに要求するもの | 目的 |
|---|---|---|---|---|
| `docs-index-sync` | ブロック | `docs/**/*.html`（diffFilter: ADR） | すべて: `docs/00_INDEX.html` | ドキュメントを追加・削除・改名したら docs/00_INDEX.html の一覧も同じコミットで更新すること。目次から辿れないドキュメントは無いのと同じになる。 |
| `app-doc-sync` | 警告 | `index.html` | いずれか: `docs/**` | index.html を変更したら docs/ の仕様書の更新要否を確認すること（このコミットには docs/ の更新が含まれていません）。 |
| `md-draft-leftover` | 警告 | `docs/**/*.md`（diffFilter: A） | （通知のみ） | docs/ のドキュメントはHTMLで持つこと。.md は下書き専用で、コミットには含めない。 |
| `doc-path-refs` | 警告 | `scripts/**`, `.claude/**`（diffFilter: ADR） | 記述された参照先が実在すること | ドキュメント中のファイル参照を、改名・削除に追従させること（存在しないファイルを指しています）。 |

<!--
  上の表は `perl scripts/devenv/check-doc-sync.pl --summary` の出力を貼ったもの。
  ルールを追加・変更したら**貼り直すこと**。手で書き足すと必ず実態からずれ、
  エージェントは書いてあることを前提に動くため、ずれはそのまま実害になる。
-->

チェックを追加・変更するときは `devenv.config.json` を編集する（スクリプトは触らない）。

- `level: "block"` は**誤検知がほぼ無いものだけ**に使う。ブロックが誤検知だらけになると `--no-verify` が常用され、全チェックが死ぬ。
- 判断を伴うもの（本当に更新が必要か人が決めるもの）は必ず `level: "warn"` にする。

## エージェントの実行記録

権限プロンプトを自動承認する運用（オートモード）では「何が実行されたか」を目で追う機会が無くなる。コードの変更は git で追えるが、**Bashコマンドそのものは git に残らない**。そこで PreToolUse フック（`.claude/hooks/audit-log.*` / `audit-log-edit.*`）が全てのBash実行とファイル編集を `.claude/logs/` に追記する。どの実行系（Perl/Node）を使うかは `.claude/settings.json` を参照。何もブロックせず記録だけ行う（gitignore済み。成果物ではないためコミットしない）。

記録は**日付ごとにファイルが分かれる**（`.claude/logs/2026-08/agent-actions-2026-08-31.log`）。1ファイルに追記し続けると点検できない大きさに育つため。

```bash
ls .claude/logs/*/                              # 日付ごとのファイル
LOG=.claude/logs/2026-08/agent-actions-2026-08-31.log
tail -100 "$LOG"                                # 直近の操作
grep -n "^[0-9].*\[!" "$LOG"                    # 印の付いた操作だけ
```

行頭の印は `devenv.config.json` の `auditLog.extraLabels` で追加できる。
**印は絞ること。** 全部に印が付くと「見返すべき操作」の目印として機能しなくなる。

記録を読んでリスクのある操作を洗い出す手順は `.claude/skills/audit-log-check/SKILL.md` にある。
**記録を取る仕組みだけあって読む手順が無いと、「記録はあるが誰も見ない」状態になる。**

## 禁止された操作

取り返しのつかない操作は、ドキュメント上の取り決めだけでなく `.claude/hooks/guard-commands.*` が実行前に拒否する。対象は `devenv.config.json` の `guardedCommands`。

<!-- 遮断されるコマンド。同じく --summary で再生成できる -->

| ID | 遮断するコマンド | 理由 |
|---|---|---|
| `git-force-push` | `git\s+push\s+(?:[^\|;&]*\s)?(?:--force(?!-with-lease)\|-f)\b` | 共有ブランチの履歴を壊す可能性があります。どうしても必要なら --force-with-lease を使い、ユーザーに確認してから実行してください。 |
| `rm-rf-root` | `\brm\s+-[a-zA-Z]*[rf][a-zA-Z]*\s+(?:/\|~\|\$HOME)(?:\s\|;\|&\|\\|\|$)` | ファイルシステムのルートまたはホームディレクトリを再帰削除しようとしています。対象パスを具体的に指定し直してください。 |
| `destroy-app-file` | `\brm\s+[^\|;&]*\b(?:index\.html\|Code\.gs)\b\|(?<!>)>\s*(?:\./)?(?:index\.html\|Code\.gs)\b` | アプリ本体(index.html)またはGASスクリプト(Code.gs)を削除・空書きしようとしています。このプロジェクトは index.html 1枚に全機能が入っているため、未コミットの状態で失うと復元できません。編集は Edit ツールで行い、全面的な書き換えが必要な場合は先に `git add -A && git commit` で退避するか、ユーザーに確認してください。 |
| `git-clean-force` | `\bgit\s+clean\s+(?:[^\|;&]*\s)?-[a-zA-Z]*f` | 未追跡ファイルを完全に削除します。git の履歴にも残らないため復元できません。まず `git clean -n` で削除対象を確認し、必要なファイルだけを個別に削除するか、ユーザーに確認してから実行してください。 |
| `git-reset-hard` | `\bgit\s+reset\s+(?:[^\|;&]*\s)?--hard\b` | 未コミットの変更を全て破棄します。編集中の index.html の内容が失われます。退避するなら `git stash`、特定ファイルだけ戻すなら `git restore <file>` を使ってください。 |
| `remove-git-dir` | `\brm\s+-[a-zA-Z]*[rf][a-zA-Z]*\s+[^\|;&]*\.git(?![\w-])` | リポジトリの全履歴が消え、復元できません。作業をやり直したいだけなら `git reset` や `git checkout` で足ります。リポジトリを作り直す必要が本当にあるなら、ユーザーに確認してください。 |
| `git-remote-add` | `\bgit\s+remote\s+(?:add\|set-url)\b` | このリポジトリを外部のリモートへ接続します。接続先が public リポジトリの場合、次の push で全履歴が一般公開され、取り消せません。knowledge/ は .gitignore で除外済みですが、それ以外に公開したくないものが無いか、接続前にユーザーと確認してください。 |
| `gh-repo-publish` | `\bgh\s+repo\s+(?:create[^\|;&]*--public\|edit[^\|;&]*--visibility\s+public)` | リポジトリを一般公開します。一度公開すると検索エンジンのキャッシュや第三者のフォークに残るため、非公開に戻しても完全な取り消しはできません。公開して良い内容かをユーザーと確認してから実行してください。 |
| `gh-repo-delete` | `\bgh\s+repo\s+delete\b` | リポジトリを完全に削除します。Issue・PR・GitHub Pages の設定も失われ、復元できません。ユーザーに確認してください。 |

<!-- この表も `perl scripts/devenv/check-doc-sync.pl --summary` の出力に含まれる。同じく貼り直すこと。 -->

設定ファイルが壊れて読めない場合でも、フック側の最低限の遮断（force push・ルート/ホームの再帰削除）は働く。
**設定の破損で保護が黙って消えることはない**が、その旨が毎回 stderr に出るので、出たら設定を直すこと。

## 開発環境の育て方

この開発環境（`devenv`）は**このリポジトリの中で育てる**。摩擦に気づいたらここで直す。

作業を完了して報告する際、開発環境そのもの（コミットゲート・フック・ドキュメント運用・スキル）に
摩擦があった場合は、成果の報告に続けて1〜2行で挙げること。例:

- ブロッキングチェックが誤検知して手戻りした
- 守るべきルールがどこに書いてあるか分からず探した
- 同じ手作業を毎回繰り返している

挙げるだけでよく、その場で直す必要はない。直すときは `/devenv-feedback` を使う。
このコマンドは**このリポジトリの devenv を直し**、その内容を `devenv.feedback.json` に記録する。

記録は大元の基盤（claude_basement）へ持ち帰るための資産である。ユーザーが任意のタイミングで
`devenv.feedback.json` を claude_basement へコピーし、そこで `/devenv-import` を実行する。
**このリポジトリから大元を直接触ることはしない**（基盤の置き場所は環境ごとに違うため）。

### 更新の受け取り

大元の基盤が更新されると、**更新パック**（`devenv-update-<版>/` フォルダ）が渡される。
中には変更されたファイルの実体と、「何をなぜ変えたか・このリポジトリで何をすべきか」を
書いた `UPDATE.md` が入っている。

リポジトリ直下に置いて Claude Code に「devenv を更新して」と伝えれば、
`.claude/skills/devenv-update` が取り込みを進める。

```bash
perl scripts/devenv/setup.pl --version   # 現在の版
cat scripts/devenv/UPDATES.md            # これまでの適用記録（あれば）
```

**全部を機械的に取り込むものではない。** 新機能を使うかはこのプロジェクトの判断であり、
取り込まなかったものは理由とともに `scripts/devenv/UPDATES.md` に残す。
理由が残らないと、次の更新のたびに同じ判断をやり直すことになる。

## 注意事項

### 保存方式の移行（2026-08-31 決定、作業中）

Google Apps Script + スプレッドシートによるクラウド保存から、**localStorage への一本化**へ移行する。端末間のデータ移動は JSON のエクスポート/インポートで手動で行う。公開先は GitHub Pages（publicリポジトリ）。

移行で撤去する対象:

- `API` オブジェクト内の `isGas` 判定と `google.script.run` の分岐
- `Code.gs`（GAS版バックエンド）
- 「クラウド保存」というUI文言（保存先がローカルになるため実態と合わない）

### 既知の不具合

**NPCの能力値が復元されない。** `API.saveScenario()` は NPCを `status`（オブジェクト）というキーで保存するが、読み込む `selectScenario()` は `statusJson` というキーを読もうとする。キー名が食い違っているため、シナリオを読み直すと能力値が `undefined` になる。保存層に手を入れる際に併せて直すこと。

### localStorage の容量制限

NPCのアバター画像を base64 の data URL としてそのまま保存しているため、画像を数枚登録するとブラウザの上限（おおむね5MB）に達する。上限を超えると `QuotaExceededError` が投げられるが、現状ハンドリングが無いため保存が黙って失敗する。

### 公開範囲

GitHub Pages（publicリポジトリ）での公開を予定している。**一度公開すると検索エンジンのキャッシュや第三者のフォークに残り、非公開に戻しても取り消せない。**

`knowledge/` はTRPGルールブック由来の資料を含むため、2026-08-31 に `.gitignore` で除外した（初回コミット前に除外したので、履歴にも残っていない）。

**リモートへの接続と公開は `guardedCommands` が実行前に遮断する。**解除して進める前に、`knowledge/` 以外に公開したくないものが無いかをユーザーと確認すること。

### clone 直後にやること

コミットゲートの配線はローカル設定のため clone には付いてこない。

```bash
git config core.hooksPath .githooks
```
